# RESTful API - Task 2: Python Consumption

## Description
This project focuses on using the Python `requests` library to fetch data from an external REST API (JSONPlaceholder) and process it into a structured format like CSV.

## Requirements
- Python 3.9
- `requests` library (`pip install requests`)

## Execution
To run the script and generate the `posts.csv` file, use:
```bash
python3 main_02_requests.py
